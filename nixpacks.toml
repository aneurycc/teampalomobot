[phases.setup]
nixPkgs = ["python313", "libpq"]

[phases.install]
dependsOn = ["setup"]
cmds = ["python -m venv .venv && .venv/bin/pip install -r requirements.txt"]

[start]
cmd = "python teampalomo/core/teampalomo_bot_FINAL.py"
